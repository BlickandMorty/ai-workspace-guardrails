name: test

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  powershell-static:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Parse scripts and validate artifacts
        shell: pwsh
        run: ./tests/Test-Static.ps1
